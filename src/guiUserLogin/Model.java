package guiUserLogin;

/** Model backing the User Login GUI (state/data used by the login view/controller). */
public class Model {
	
	/** Creates a new login model instance. */
	public Model() { }

}
