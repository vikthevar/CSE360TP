package guiUserUpdate;

/** Model backing the User Update GUI (state/data used by the update view/controller). */
public class Model {

	/** Creates a new user update model instance. */
	public Model() { }
}
